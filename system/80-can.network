# Activate can0, can1 and can2 on boot using systemd-networkd
# File location: /etc/systemd/network/80-can.network
#
# Enable systemd-networkd: sudo systemctl enable systemd-networkd
#
[Match]
Name=can0 can1 can2

[CAN]
BitRate=500K
RestartSec=100ms
